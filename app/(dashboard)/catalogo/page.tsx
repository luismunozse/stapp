import { CatalogoAdmin } from "@/components/catalogo/catalogo-admin"

export const metadata = {
  title: "Catálogo público — STApp",
  description: "Gestiona el catálogo de productos y servicios que compartís con tus clientes",
}

export default function CatalogoPage() {
  return <CatalogoAdmin />
}
